# 🔧 Техническая документация YESS!Partner

## Архитектура системы

### Backend (FastAPI)

#### Структура проекта

```
yess-backend/
├── app/
│   ├── api/              # API endpoints (v1, v2)
│   │   ├── v1/
│   │   │   ├── auth.py
│   │   │   ├── users.py
│   │   │   ├── partners.py
│   │   │   ├── transactions.py
│   │   │   └── admin.py
│   │   └── v2/
│   ├── core/             # Ядро приложения
│   │   ├── config.py     # Конфигурация
│   │   ├── security.py   # JWT, хеширование
│   │   ├── database.py   # Подключение к БД
│   │   └── error_handler.py  # Обработка ошибок
│   ├── models/           # SQLAlchemy модели
│   ├── schemas/          # Pydantic схемы
│   ├── services/         # Бизнес-логика
│   └── main.py           # Точка входа
├── alembic/              # Миграции БД
├── tests/                # Тесты
└── requirements.txt      # Зависимости
```

#### Основные компоненты

**1. Аутентификация и авторизация**
- JWT токены (access + refresh)
- Bcrypt для хеширования паролей
- Role-based access control (RBAC)
- Rate limiting

**2. База данных**
- PostgreSQL как основная БД
- SQLAlchemy ORM
- Alembic для миграций
- Connection pooling

**3. Кэширование**
- Redis для кэширования
- TTL для автоматической инвалидации
- Circuit breaker pattern

**4. Безопасность**
- CORS настройки
- SQL injection защита
- XSS защита
- CSRF токены
- Rate limiting

### Frontend (React + TypeScript)

#### Admin Panel

**Технологии:**
- React 18 с Hooks
- TypeScript 5.0+
- Vite для сборки
- Ant Design для UI
- React Query для state management
- React Router для навигации

**Структура:**
```
admin-panel/src/
├── components/      # Переиспользуемые компоненты
├── pages/          # Страницы приложения
├── services/       # API клиенты
├── hooks/          # Custom hooks
├── store/          # State management
├── styles/         # Глобальные стили
└── utils/          # Утилиты
```

**Основные страницы:**
- Dashboard - главная страница с аналитикой
- Users - управление пользователями
- Partners - управление партнерами
- Transactions - история транзакций
- Promotions - управление акциями
- Settings - настройки системы

#### Partner Panel

**Технологии:**
- React 18 с Hooks
- TypeScript 5.0+
- Vite для сборки
- Ant Design для UI
- React Query для state management
- React Router для навигации

**Структура:**
```
partner-panel/src/
├── components/      # Переиспользуемые компоненты
├── pages/          # Страницы приложения
├── services/       # API клиенты
├── hooks/          # Custom hooks
├── store/          # State management
└── styles/         # Глобальные стили
```

**Основные страницы:**
- Dashboard - статистика и аналитика
- Profile - профиль партнера
- Locations - управление локациями
- Promotions - управление акциями
- Transactions - история транзакций
- Employees - управление сотрудниками
- Integrations - API ключи и интеграции
- Billing - биллинг и платежи

### Mobile App (.NET MAUI)

**Технологии:**
- .NET MAUI
- C# 10+
- MVVM паттерн
- HTTP клиент для API

**Структура:**
```
YessLoyaltyApp/
├── Services/        # Сервисы (API, Auth, Storage)
├── ViewModels/     # ViewModels для MVVM
├── Views/          # XAML страницы
└── Models/         # Модели данных
```

## API Endpoints

### Аутентификация

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/v1/auth/register` | Регистрация пользователя |
| POST | `/api/v1/auth/login` | Вход в систему |
| POST | `/api/v1/auth/refresh` | Обновление токена |
| POST | `/api/v1/auth/logout` | Выход из системы |

### Пользователи

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/v1/users/me` | Получить профиль |
| PUT | `/api/v1/users/me` | Обновить профиль |
| GET | `/api/v1/users/{id}` | Получить пользователя (admin) |

### Партнеры

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/v1/partners` | Список партнеров |
| GET | `/api/v1/partners/{id}` | Детали партнера |
| GET | `/api/v1/partners/nearby` | Партнеры рядом |
| POST | `/api/v1/partners` | Создать партнера (admin) |
| PUT | `/api/v1/partners/{id}` | Обновить партнера (admin) |

### Транзакции

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/v1/transactions` | История транзакций |
| POST | `/api/v1/transactions` | Создать транзакцию |
| GET | `/api/v1/transactions/{id}` | Детали транзакции |

### Кошелек

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/v1/wallet` | Информация о кошельке |
| GET | `/api/v1/wallet/balance` | Текущий баланс |
| POST | `/api/v1/wallet/topup` | Пополнить баланс |

### Admin API

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/v1/admin/users` | Список пользователей |
| GET | `/api/v1/admin/partners` | Список партнеров |
| GET | `/api/v1/admin/analytics` | Аналитика |
| POST | `/api/v1/admin/notifications` | Отправить уведомление |

### Partner API

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/v1/partner/me` | Профиль партнера |
| GET | `/api/v1/partner/dashboard/stats` | Статистика |
| GET | `/api/v1/partner/locations` | Локации партнера |
| POST | `/api/v1/partner/locations` | Создать локацию |
| GET | `/api/v1/partner/promotions` | Акции партнера |
| POST | `/api/v1/partner/promotions` | Создать акцию |
| GET | `/api/v1/partner/transactions` | Транзакции партнера |
| GET | `/api/v1/partner/employees` | Сотрудники |

## База данных

### Основные таблицы

**users** - Пользователи
- id, phone, email, name, password_hash
- balance, referral_code, is_active
- created_at, updated_at

**partners** - Партнеры
- id, name, category, description
- cashback_rate, logo_url, phone, email
- latitude, longitude, is_verified, is_active
- created_at, updated_at

**partner_locations** - Локации партнеров
- id, partner_id, name, address
- latitude, longitude, phone, email
- working_hours, status
- created_at, updated_at

**transactions** - Транзакции
- id, user_id, partner_id, type
- amount, status, balance_before, balance_after
- payment_method, payment_id
- created_at, updated_at

**promotions** - Акции
- id, partner_id, title, description
- discount_percent, start_date, end_date
- status, priority
- created_at, updated_at

**notifications** - Уведомления
- id, user_id, title, message
- type, is_read
- created_at

## Безопасность

### JWT Токены

- **Access Token**: короткоживущий (30 минут)
- **Refresh Token**: долгоживущий (7 дней)
- Алгоритм: HS256
- Хранение: HTTP-only cookies (рекомендуется) или localStorage

### Хеширование паролей

- Алгоритм: bcrypt
- Salt rounds: 12

### Rate Limiting

- По умолчанию: 100 запросов/минуту на IP
- Для аутентификации: 5 попыток/минуту
- Настраивается через Redis

### CORS

Разрешенные origins:
- `http://localhost:3001` (Admin Panel)
- `http://localhost:3002` (Partner Panel)
- Production URLs (настраиваются в .env)

## Мониторинг

### Health Checks

- `GET /health` - общая проверка
- `GET /health/db` - проверка БД
- `GET /health/cache` - проверка Redis

### Метрики Prometheus

- HTTP запросы (количество, время ответа)
- Ошибки (4xx, 5xx)
- Активные соединения к БД
- Использование памяти/CPU

### Алерты

- Высокий уровень ошибок (>5%)
- Медленные запросы (>1s)
- Недоступность БД
- Высокое использование ресурсов

## Деплой

### Docker

```bash
# Development
docker-compose -f docker-compose.yml up

# Production
docker-compose -f docker-compose.prod.yml up -d
```

### Kubernetes

```bash
# Применить конфигурации
kubectl apply -f k8s/

# Проверить статус
kubectl get pods
kubectl get services
```

## Тестирование

### Backend тесты

```bash
cd yess-backend
pytest tests/ -v --cov=app
```

### Frontend тесты

```bash
cd admin-panel
npm test

cd partner-panel
npm test
```

### E2E тесты

```bash
# Используйте Playwright или Cypress
npm run test:e2e
```

## Производительность

### Оптимизации

- **Database**: индексы на часто используемых полях
- **Caching**: Redis для кэширования запросов
- **Connection Pooling**: пул соединений к БД
- **Lazy Loading**: загрузка данных по требованию
- **Pagination**: пагинация для больших списков

### Рекомендации

- Используйте CDN для статических файлов
- Включите gzip сжатие
- Настройте кэширование на уровне Nginx
- Используйте Redis для сессий

## Troubleshooting

### Частые проблемы

**1. Ошибка подключения к БД**
- Проверьте DATABASE_URL в .env
- Убедитесь, что PostgreSQL запущен
- Проверьте права доступа пользователя

**2. Ошибка CORS**
- Проверьте CORS_ORIGINS в .env
- Убедитесь, что frontend URL добавлен

**3. Ошибка JWT токена**
- Проверьте SECRET_KEY в .env
- Убедитесь, что токен не истек
- Проверьте формат токена

**4. Медленные запросы**
- Проверьте индексы в БД
- Включите кэширование Redis
- Оптимизируйте SQL запросы

## Контакты

Для вопросов и поддержки:
- GitHub Issues: [Создать issue](https://github.com/Amanch1ik/PANEL-s_YESS-Go/issues)

