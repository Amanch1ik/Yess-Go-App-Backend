# 👥 Workflow для команды


## 🔄 Процесс работы

### 📅 Перед началом работы каждый день:

1. **Обновите код из репозитория:**
   ```bash
   git pull origin develop
   # или
   git pull origin main
   ```

2. **Создайте/обновите .env файлы:**
   ```bash
   # Admin Panel
   cd Yess-Money---app-master/admin-panel
   cp .env.example .env
   # Отредактируйте .env при необходимости
   
   # Partner Panel
   cd ../partner-panel
   cp .env.example .env
   # Отредактируйте .env при необходимости
   ```

3. **Проверьте, что бэкенд запущен:**
   ```bash
   # Проверка здоровья API
   curl http://localhost:8000/health
   # или
   curl http://localhost:8000/api/v1/health
   ```

4. **Проверьте CHANGELOG.md** на наличие новых изменений

### 💻 Работа над изменениями:

1. **Создайте ветку для вашей задачи:**
   ```bash
   # Для панелей
   git checkout -b feature/panels/your-feature-name
   
   # Для мобильного приложения
   git checkout -b feature/mobile/your-feature-name
   
   # Для бэкенда
   git checkout -b feature/backend/your-feature-name
   ```

2. **Делайте изменения в коде**

3. **Коммитьте часто и с понятными сообщениями:**
   ```bash
   git add .
   git commit -m "feat: добавлена функция управления промо-акциями"
   # или
   git commit -m "fix: исправлена ошибка отображения валют"
   # или
   git commit -m "docs: обновлена документация API"
   ```

4. **Пушите в свою ветку:**
   ```bash
   git push origin feature/panels/your-feature-name
   ```

### ✅ После завершения работы:

1. **Убедитесь, что код работает:**
   - Протестируйте локально
   - Проверьте, что нет ошибок линтера
   - Убедитесь, что все тесты проходят

2. **Обновите CHANGELOG.md:**
   - Добавьте описание ваших изменений в секцию `[Unreleased]`
   - Используйте правильные категории (Added, Changed, Fixed, etc.)

3. **Создайте Pull Request:**
   - Перейдите на GitHub
   - Создайте Pull Request из вашей ветки в `develop`
   - Заполните шаблон PR:
     - Что изменилось?
     - Почему это нужно?
     - Как протестировать?
     - Скриншоты (если применимо)

4. **Уведомите команду:**
   - Создайте issue или напишите в чат команды
   - Укажите номер PR и краткое описание

## ⚠️ Важно! Критические моменты

### 🗄️ Если вы изменяете БД (модели, схемы):

1. **Создайте миграцию Alembic:**
   ```bash
   cd yess-backend
   alembic revision --autogenerate -m "описание изменений"
   alembic upgrade head
   ```

2. **Обновите модели в `yess-backend/app/models/`**

3. **Обновите схемы Pydantic в `yess-backend/app/schemas/`**

4. **Уведомите команду ОБЯЗАТЕЛЬНО:**
   - Создайте issue с описанием изменений
   - Укажите, какие таблицы/поля изменились
   - Предоставьте SQL миграции

5. **Обновите документацию:**
   - `BACKEND_INTEGRATION.md`
   - Swagger документацию (автоматически через FastAPI)

### 🔌 Если вы добавляете новый API endpoint:

1. **Создайте endpoint в бэкенде:**
   ```python
   @router.post("/new-endpoint")
   async def new_endpoint(data: Schema):
       # ваш код
   ```

2. **Обновите документацию:**
   - `BACKEND_INTEGRATION.md` - добавьте описание endpoint
   - Swagger обновится автоматически

3. **Обновите типы в панелях/приложении:**
   - Если изменились типы данных, обновите TypeScript/C# типы
   - Обновите API сервисы

4. **Уведомите команду:**
   - Создайте issue или сообщение в чате
   - Укажите URL endpoint и примеры запросов/ответов

### 📊 Если вы изменяете схемы данных (DTO, типы):

1. **Обновите схемы в бэкенде:**
   - `yess-backend/app/schemas/`

2. **Обновите типы во фронтенде:**
   - Admin Panel: `admin-panel/src/types/`
   - Partner Panel: `partner-panel/src/types/`
   - Mobile App: соответствующие файлы типов

3. **Обновите API сервисы:**
   - Admin Panel: `admin-panel/src/services/`
   - Partner Panel: `partner-panel/src/services/`
   - Mobile App: соответствующие сервисы

4. **Уведомите команду:**
   - Создайте issue с описанием изменений
   - Укажите, какие поля добавлены/изменены/удалены

## 🔀 Git Workflow

### Структура веток:

```
main/master (production-ready код)
  ├── develop (общая ветка разработки)
  │   ├── feature/panels/* (ваши изменения панелей)
  │   ├── feature/mobile/* (изменения мобильного приложения)
  │   └── feature/backend/* (изменения бэкенда)
  └── hotfix/* (срочные исправления)
```

### Правила работы с ветками:

1. **Всегда работайте в feature ветках**, не коммитьте напрямую в `develop` или `main`
2. **Синхронизируйтесь регулярно:**
   ```bash
   git checkout develop
   git pull origin develop
   git checkout feature/your-branch
   git merge develop  # или git rebase develop
   ```
3. **Решайте конфликты сразу**, не откладывайте
4. **Удаляйте старые ветки после мержа:**
   ```bash
   git branch -d feature/old-branch
   git push origin --delete feature/old-branch
   ```

## 📝 Соглашения о коммитах

Используйте понятные сообщения коммитов:

### Формат:
```
тип(область): краткое описание

Подробное описание (если нужно)
```

### Типы:
- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление задач сборки, настроек и т.д.

### Примеры:
```bash
git commit -m "feat(panels): добавлена страница управления промо-акциями"
git commit -m "fix(backend): исправлена ошибка валидации email"
git commit -m "docs(api): обновлена документация endpoints транзакций"
git commit -m "refactor(panels): оптимизирована загрузка данных dashboard"
```

## 🧪 Тестирование

### Перед коммитом проверьте:

1. **Локальное тестирование:**
   - Запустите панели и проверьте функциональность
   - Проверьте, что API запросы работают
   - Убедитесь, что нет ошибок в консоли

2. **Линтер:**
   ```bash
   # Admin Panel
   cd admin-panel
   npm run lint
   
   # Partner Panel
   cd partner-panel
   npm run lint
   ```

3. **Типы TypeScript:**
   ```bash
   npm run type-check
   ```

## 📞 Связь и координация

### Когда создавать Issue:

- ✅ Обсуждение больших изменений
- ✅ Сообщение о багах
- ✅ Запрос новой функциональности
- ✅ Вопросы по архитектуре
- ✅ Изменения в БД или API

### Когда создавать Pull Request:

- ✅ После завершения задачи
- ✅ Когда код готов к ревью
- ✅ Когда нужно обсудить реализацию

### Обязательно уведомляйте команду:

- 🔴 Изменения в структуре БД
- 🔴 Новые/измененные API endpoints
- 🔴 Изменения в схемах данных
- 🔴 Критические исправления
- 🟡 Новые зависимости
- 🟡 Изменения в конфигурации

## 🚀 Быстрый старт для новых участников

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/Amanch1ik/Yess-Go-App-Backend.git
   cd Yess-Go-App-Backend
   ```

2. **Настройте окружение:**
   ```bash
   # Создайте .env файлы из примеров
   cp admin-panel/.env.example admin-panel/.env
   cp partner-panel/.env.example partner-panel/.env
   ```

3. **Установите зависимости:**
   ```bash
   # Backend
   cd yess-backend
   python -m venv venv
   source venv/bin/activate  # или venv\Scripts\activate на Windows
   pip install -r requirements.txt
   
   # Admin Panel
   cd ../admin-panel
   npm install
   
   # Partner Panel
   cd ../partner-panel
   npm install
   ```

4. **Запустите проект:**
   ```bash
   # Следуйте инструкциям в README.md или QUICK_START_PANELS.md
   ```

## 📚 Полезные ссылки

- [README.md](./README.md) - общая информация о проекте
- [BACKEND_INTEGRATION.md](./BACKEND_INTEGRATION.md) - интеграция с бэкендом
- [QUICK_START_PANELS.md](./QUICK_START_PANELS.md) - быстрый старт панелей
- [TECHNICAL.md](./TECHNICAL.md) - техническая документация
- [CHANGELOG.md](./CHANGELOG.md) - история изменений

## ❓ FAQ

**Q: Что делать, если я случайно закоммитил в develop?**
A: Создайте новую ветку из этого коммита, затем сбросьте develop:
```bash
git checkout -b feature/save-my-work
git checkout develop
git reset --hard origin/develop
```

**Q: Как синхронизироваться с изменениями друга?**
A: Регулярно делайте `git pull origin develop` и мержите изменения в свою ветку.

**Q: Что делать при конфликтах?**
A: Решайте конфликты сразу, обсудите с командой если нужно. Не откладывайте.

**Q: Можно ли работать над одной задачей вместе?**
A: Да, используйте общую feature ветку и координируйте работу через Issues/PR.

---

**Последнее обновление:** 2025-01-XX
**Версия:** 1.0.0

