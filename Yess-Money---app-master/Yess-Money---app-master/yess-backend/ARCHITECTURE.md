# 🏗️ Архитектура системы аутентификации

## 📊 Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                        CLIENT                               │
│  (Frontend / Mobile App / Postman / cURL)                  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    API ENDPOINTS                            │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  POST /api/v1/auth/register                          │  │
│  │  POST /api/v1/auth/login                             │  │
│  │  GET  /api/v1/auth/me  [Protected]                   │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                   MIDDLEWARE LAYER                          │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────┐  │
│  │  CORS          │  │  Rate Limit    │  │  Security    │  │
│  └────────────────┘  └────────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│               AUTH SERVICE LAYER                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  app/api/v1/auth.py                                    │ │
│  │  ├─ register_user()                                    │ │
│  │  ├─ login_user()                                       │ │
│  │  └─ get_me()                                           │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                ▼                       ▼
┌──────────────────────────┐  ┌──────────────────────────┐
│  BUSINESS LOGIC          │  │  DEPENDENCIES            │
│  app/services/           │  │  app/services/           │
│  auth_service.py         │  │  dependencies.py         │
│                          │  │                          │
│  ├─ register_user()      │  │  └─ get_current_user()   │
│  ├─ authenticate_user()  │  │     (JWT Decode)         │
│  ├─ create_access_token()│  │                          │
│  ├─ verify_password()    │  │                          │
│  └─ get_password_hash()  │  │                          │
└──────────────────────────┘  └──────────────────────────┘
                │                         │
                └─────────┬───────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                   DATABASE LAYER                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  app/models/user.py                                    │ │
│  │                                                        │ │
│  │  User Model:                                           │ │
│  │  ├─ id (PK)                                            │ │
│  │  ├─ phone (номер телефона)                            │ │
│  │  ├─ password_hash (хешированный пароль)               │ │
│  │  ├─ first_name                                         │ │
│  │  ├─ last_name                                          │ │
│  │  ├─ email                                              │ │
│  │  └─ ... (другие поля)                                 │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
                    ┌──────────────┐
                    │  PostgreSQL  │
                    │   Database   │
                    └──────────────┘
```

## 🔄 Поток данных

### 1️⃣ Регистрация (POST /auth/register)

```
Client Request:
{
  "phone_number": "+996555123456",
  "password": "secure_password",
  "first_name": "Иван",
  "last_name": "Иванов"
}
        │
        ▼
app/api/v1/auth.py
  register_user()
        │
        ▼
app/services/auth_service.py
  AuthService.register_user()
    ├─ Проверка существования пользователя
    ├─ Хеширование пароля (bcrypt)
    └─ Создание записи в БД
        │
        ▼
app/models/user.py
  User(
    phone=phone_number,
    password_hash=hashed_password,
    first_name=first_name,
    last_name=last_name
  )
        │
        ▼
PostgreSQL Database
  INSERT INTO users ...
        │
        ▼
Client Response:
{
  "id": 1,
  "phone_number": "+996555123456",
  "first_name": "Иван",
  "last_name": "Иванов",
  ...
}
```

### 2️⃣ Вход (POST /auth/login)

```
Client Request:
username=+996555123456
password=secure_password
        │
        ▼
app/api/v1/auth.py
  login_user()
        │
        ▼
app/services/auth_service.py
  AuthService.authenticate_user()
    ├─ Поиск пользователя по phone
    ├─ Проверка пароля (bcrypt.verify)
    └─ Создание JWT токена
        │
        ▼
app/services/auth_service.py
  AuthService.create_access_token()
    ├─ Payload: {"sub": user_id, "exp": expiry}
    ├─ Алгоритм: HS256
    └─ Подпись: SECRET_KEY
        │
        ▼
Client Response:
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

### 3️⃣ Получение данных пользователя (GET /auth/me)

```
Client Request:
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
        │
        ▼
app/services/dependencies.py
  get_current_user()
    ├─ Извлечение токена из заголовка
    ├─ Декодирование JWT
    │   ├─ Проверка подписи (SECRET_KEY)
    │   ├─ Проверка срока действия
    │   └─ Извлечение user_id из payload
    └─ Загрузка пользователя из БД
        │
        ▼
PostgreSQL Database
  SELECT * FROM users WHERE id = user_id
        │
        ▼
app/api/v1/auth.py
  get_me(current_user)
        │
        ▼
Client Response:
{
  "id": 1,
  "phone_number": "+996555123456",
  "first_name": "Иван",
  "last_name": "Иванов",
  ...
}
```

## 📦 Структура файлов

```
yess-backend/
│
├── app/
│   ├── api/
│   │   └── v1/
│   │       ├── api_router.py          # Главный роутер API
│   │       └── auth.py                # Эндпоинты аутентификации
│   │
│   ├── services/
│   │   ├── auth_service.py            # Бизнес-логика аутентификации
│   │   └── dependencies.py            # 🆕 JWT декодирование
│   │
│   ├── models/
│   │   └── user.py                    # Модель пользователя
│   │
│   ├── schemas/
│   │   └── user.py                    # Pydantic схемы
│   │
│   ├── core/
│   │   ├── config.py                  # Конфигурация (SECRET_KEY)
│   │   └── database.py                # Подключение к БД
│   │
│   └── main.py                        # Точка входа FastAPI
│
├── alembic/
│   └── versions/
│       └── 2025_11_05_0000-*.py       # 🆕 Миграция
│
├── test_auth_api.py                   # 🆕 Автотесты
├── start_fixed_auth.ps1               # 🆕 Скрипт запуска
│
└── 📚 Документация:
    ├── README_AUTH_FIX.md             # Краткая сводка
    ├── QUICK_START.md                 # Быстрый старт
    ├── AUTH_SETUP.md                  # Подробная документация
    └── AUTH_FIX_SUMMARY.md            # Полный отчет
```

## 🔐 Безопасность

### Хеширование паролей
```python
# Используется bcrypt
from passlib.context import CryptContext
pwd_context = CryptContext(schemes=["bcrypt"])

# Хеширование
hashed = pwd_context.hash("plain_password")

# Проверка
is_valid = pwd_context.verify("plain_password", hashed)
```

### JWT токены
```python
# Структура токена
{
  "header": {
    "alg": "HS256",
    "typ": "JWT"
  },
  "payload": {
    "sub": "user_id",
    "exp": "timestamp"
  },
  "signature": "..."
}

# Создание
jwt.encode(payload, SECRET_KEY, algorithm="HS256")

# Декодирование
jwt.decode(token, SECRET_KEY, algorithms=["HS256"])
```

## 🔧 Конфигурация

### Переменные окружения (.env)
```env
DATABASE_URL=postgresql://user:pass@host/db
SECRET_KEY=your_secret_key_32_chars_minimum
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

### Настройки в config.py
```python
class Settings(BaseSettings):
    SECRET_KEY: str
    ALGORITHM: str = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 30
    
    class Config:
        env_file = ".env"
```

## 📊 База данных

### Таблица users
```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    phone VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(255),
    first_name VARCHAR(255),      -- 🆕 Добавлено
    last_name VARCHAR(255),        -- 🆕 Добавлено
    name VARCHAR(255),             -- Для обратной совместимости
    email VARCHAR(255) UNIQUE,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT NOW(),
    ...
);
```

## 🎯 Точки расширения

### Дальнейшие улучшения:

1. **Refresh Tokens**
   - Добавить таблицу refresh_tokens
   - Реализовать эндпоинт /auth/refresh

2. **SMS Верификация**
   - Интеграция с SMS сервисом
   - Добавить verification_code в User

3. **Email Верификация**
   - Email сервис (SMTP)
   - Эндпоинт /auth/verify-email

4. **Восстановление пароля**
   - Генерация токена сброса
   - Эндпоинт /auth/reset-password

5. **Социальные входы**
   - OAuth2 провайдеры (Google, Facebook)
   - Эндпоинты /auth/google, /auth/facebook

6. **Rate Limiting**
   - Ограничение попыток входа
   - Защита от brute-force атак

## ✅ Текущий статус

**Реализовано:**
- ✅ JWT авторизация
- ✅ Регистрация пользователей
- ✅ Вход в систему
- ✅ Получение данных текущего пользователя
- ✅ Хеширование паролей
- ✅ Валидация токенов
- ✅ Документация API

**Готово к использованию!** 🎉

