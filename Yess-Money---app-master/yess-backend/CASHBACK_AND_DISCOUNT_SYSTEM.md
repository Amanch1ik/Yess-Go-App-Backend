# Система кэшбэка и скидок YessGo

## 📋 Обзор

Система YessGo работает по принципу взаимовыгодного партнёрства:
- **Партнёры** предоставляют скидки клиентам YessGo
- **Клиенты** получают скидки, используя Yess Coin
- **Партнёры** обменивают предоставленную скидку на Yess Coin (кэшбэк для клиента)

## 🔄 Бизнес-логика

### 1. Скидка (Discount)

**Что это:**
- Скидка, которую партнёр предоставляет клиентам YessGo при покупке
- Клиент платит меньше за товары/услуги

**Как работает:**
- Партнёр договаривается с YessGo о максимальном проценте скидки (`max_discount_percent`)
- Клиент использует Yess Coin для получения скидки
- Скидка списывается с баланса Yess Coin клиента

**Пример:**
```
Сумма покупки: 1000 сом
Максимальная скидка партнёра: 10%
Скидка: 100 сом (10% от 1000)
Клиент платит: 900 сом
Yess Coin списано: 100 сом
```

### 2. Кэшбэк (Cashback)

**Что это:**
- Yess Coin, которые клиент получает обратно после покупки
- Начисляются на баланс клиента автоматически

**Как работает:**
- После покупки клиент получает кэшбэк в Yess Coin
- Процент кэшбэка определяется партнёром (`cashback_rate` или `default_cashback_rate`)
- Кэшбэк начисляется на баланс клиента

**Пример:**
```
Сумма покупки (после скидки): 900 сом
Кэшбэк партнёра: 5%
Кэшбэк начислено: 45 Yess Coin
```

### 3. Обмен скидки на Yess Coin

**Бизнес-модель:**
- Партнёр предоставляет скидку клиенту (например, 10%)
- Клиент платит меньше за товары/услуги
- Взамен клиент получает кэшбэк в Yess Coin (например, 5%)
- Это создаёт цикл лояльности: клиент тратит Yess Coin на скидки, получает Yess Coin обратно

**Полный цикл транзакции:**
```
1. Клиент делает покупку на 1000 сом
2. Применяется скидка 10% (100 сом) - списывается с баланса Yess Coin
3. Клиент платит 900 сом партнёру
4. Начисляется кэшбэк 5% от 900 сом = 45 Yess Coin
5. Итого: клиент потратил 100 Yess Coin, получил 45 Yess Coin обратно
```

## 💾 Модель данных

### Partner (Партнёр)

**Поля, связанные со скидками и кэшбэком:**

```python
# Максимальная скидка, которую партнёр может предоставить клиентам
max_discount_percent = Column(Numeric(5, 2), nullable=False)  # Например, 10.00

# Процент кэшбэка, который партнёр предоставляет клиентам
cashback_rate = Column(Numeric(5, 2), default=5.0)  # Например, 5.00

# Базовый процент кэшбэка (используется, если cashback_rate не указан)
default_cashback_rate = Column(Float, default=5.0)  # Например, 5.0
```

**Ограничения:**
- `max_discount_percent`: от 0 до 100%
- `cashback_rate`: от 0 до 100%

### Transaction (Транзакция)

**Поля для отслеживания операций:**

```python
# Yess Coin, потраченные клиентом (скидка)
yescoin_used = Column(Numeric(10, 2), default=0.0)

# Yess Coin, заработанные клиентом (кэшбэк)
yescoin_earned = Column(Numeric(10, 2), default=0.0)

# Тип транзакции: "payment", "discount", "bonus", "refund"
type = Column(String(50), nullable=False)
```

### Wallet (Кошелёк)

**Баланс клиента в Yess Coin:**

```python
balance = Column(Numeric(10, 2), default=0.00, nullable=False)
```

## 🔧 API Endpoints

### Расчет скидки

**POST `/api/v1/orders/calculate`**

Рассчитывает скидку для заказа:
- Проверяет максимальную скидку партнёра
- Рассчитывает итоговую сумму после скидки

**Пример запроса:**
```json
{
  "partner_id": 12,
  "order_total": 1000.00
}
```

**Пример ответа:**
```json
{
  "discount": 100.00,
  "final_amount": 900.00,
  "max_discount_percent": 10.0
}
```

### Подтверждение заказа

**POST `/api/v1/orders/confirm`**

Подтверждает заказ и применяет скидку:
- Списывает Yess Coin с баланса клиента (скидка)
- Создаёт запись о транзакции
- Начисляет кэшбэк (если реализовано)

### Оплата через QR

**POST `/api/v1/qr/pay`**

Обрабатывает оплату через QR-код:
1. Рассчитывает скидку
2. Списывает Yess Coin (скидка)
3. Начисляет кэшбэк
4. Создаёт транзакцию

**Логика из кода:**
```python
# Рассчитываем скидку
discount_percent = float(partner.max_discount_percent)
discount_amount = request.amount * (discount_percent / 100)

# Итоговая сумма к списанию
final_amount = request.amount - discount_amount

# Списываем Yess Coin
wallet.yescoin_balance -= final_amount

# Рассчитываем и начисляем кэшбэк
cashback_percent = float(partner.cashback_rate)
cashback_amount = final_amount * (cashback_percent / 100)
wallet.yescoin_balance += cashback_amount
```

## 📊 Примеры использования

### Пример 1: Покупка в ресторане

**Партнёр:** Ресторан "Нават"
- `max_discount_percent`: 10%
- `cashback_rate`: 5%

**Транзакция:**
1. Клиент делает заказ на 2000 сом
2. Скидка 10% = 200 сом (списывается с баланса Yess Coin)
3. Клиент платит 1800 сом ресторану
4. Кэшбэк 5% от 1800 = 90 Yess Coin начисляется клиенту

**Итого:**
- Клиент потратил: 200 Yess Coin (скидка)
- Клиент получил: 90 Yess Coin (кэшбэк)
- Чистые расходы: 110 Yess Coin
- Экономия: 200 сом (скидка от покупки)

### Пример 2: Покупка в кафе

**Партнёр:** Кафе "CoffeeTime"
- `max_discount_percent`: 15%
- `cashback_rate`: 7%

**Транзакция:**
1. Клиент делает заказ на 500 сом
2. Скидка 15% = 75 сом (списывается с баланса Yess Coin)
3. Клиент платит 425 сом кафе
4. Кэшбэк 7% от 425 = 29.75 Yess Coin начисляется клиенту

**Итого:**
- Клиент потратил: 75 Yess Coin (скидка)
- Клиент получил: 29.75 Yess Coin (кэшбэк)
- Чистые расходы: 45.25 Yess Coin
- Экономия: 75 сом (скидка от покупки)

## 🎯 Ключевые моменты для разработчиков

### 1. Приоритет полей кэшбэка

При отображении кэшбэка используйте следующую логику:
```python
# Если есть персонализированный кэшбэк
if partner.cashback_rate:
    cashback_rate = partner.cashback_rate
# Иначе используем базовый
else:
    cashback_rate = partner.default_cashback_rate
```

### 2. Валидация скидки

Всегда проверяйте, что скидка не превышает максимальную:
```python
max_discount_by_percent = order_total * (partner.max_discount_percent / 100)
if discount > max_discount_by_percent:
    raise HTTPException(
        status_code=409,
        detail=f"Discount exceeds partner's maximum ({partner.max_discount_percent}%)"
    )
```

### 3. Атомарность транзакций

Все операции с балансом должны быть атомарными:
- Используйте транзакции базы данных
- Откатывайте изменения при ошибках
- Сохраняйте баланс до и после операции

### 4. Отображение на фронтенде

На странице деталей партнёра отображайте:
- **Кэшбэк**: процент, который клиент получит обратно
- **Макс. скидка**: максимальный процент скидки, который можно получить

**Пример из PartnerDetailPage.xaml:**
```xml
<Label x:Name="CashbackLabel" Text="5%"/>  <!-- Кэшбэк -->
<Label x:Name="DiscountLabel" Text="10%"/> <!-- Макс. скидка -->
```

## 📝 Примечания

1. **Скидка списывается с баланса Yess Coin** - клиент должен иметь достаточный баланс
2. **Кэшбэк начисляется автоматически** - после успешной транзакции
3. **Проценты могут быть разными** - каждый партнёр устанавливает свои условия
4. **Максимальная скидка ограничена** - партнёр не может предоставить больше, чем договорено

## 🔗 Связанные файлы

- `app/models/partner.py` - модель партнёра
- `app/models/transaction.py` - модель транзакции
- `app/models/wallet.py` - модель кошелька
- `app/api/v1/qr.py` - обработка оплаты через QR
- `app/api/v1/order.py` - обработка заказов
- `YessGoFrontV2/Views/PartnerDetailPage.xaml` - отображение информации о партнёре

## ❓ Часто задаваемые вопросы

**Q: Почему клиент тратит Yess Coin на скидку, но получает меньше обратно?**
A: Это нормальная бизнес-модель. Клиент получает скидку на покупку (экономит деньги), а кэшбэк - это бонус за использование системы.

**Q: Может ли партнёр изменить проценты?**
A: Да, через админ-панель или API, но изменения применяются к новым транзакциям.

**Q: Что происходит, если у клиента недостаточно Yess Coin?**
A: Транзакция отклоняется с ошибкой "Insufficient balance". Клиент должен пополнить баланс.

**Q: Можно ли использовать скидку без Yess Coin?**
A: Нет, скидка предоставляется только при использовании Yess Coin. Это часть бизнес-модели системы.

---

**Последнее обновление:** 2025-01-09
**Автор:** AI Assistant
**Версия:** 1.0

