# 🔧 Устранение проблем с экспортом

## Проблема: Файл экспортируется пустым

### Причины и решения

#### 1. Нет данных для экспорта

**Симптомы:** Файл создается, но пустой

**Решение:**
- Убедитесь, что в таблице есть данные
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что API возвращает данные

#### 2. Неправильная структура данных

**Симптомы:** Данные не попадают в файл

**Решение:**
- Откройте консоль браузера (F12)
- При экспорте проверьте логи:
  - `Exporting CSV:` - показывает количество строк и колонок
  - `First cell value:` - показывает первое значение
  - `CSV export summary:` - показывает содержимое файла

#### 3. Excel не распознает формат

**Симптомы:** Файл открывается, но данные в одной колонке

**Решение:**
1. Откройте файл в Excel
2. Перейдите в "Данные" → "Получить данные" → "Из файла" → "Из текста/CSV"
3. Выберите файл
4. В мастере импорта:
   - Разделитель: **Точка с запятой (;)**
   - Кодировка: **UTF-8**
   - Нажмите "Загрузить"

Или:

1. Откройте файл в Блокноте
2. Убедитесь, что данные разделены точкой с запятой
3. Сохраните файл
4. Откройте в Excel через "Данные" → "Из текста/CSV"

## Проверка экспорта

### В консоли браузера вы увидите:

```
Exporting CSV: { rows: 20, columns: 6, headers: [...] }
First cell value: { key: 'id', value: 1, displayValue: '1', escapedValue: '1' }
First row: "1;Partner 1;retail;partner1@example.com;..."
CSV export summary: { totalRows: 21, headerRow: "...", firstDataRow: "...", ... }
File downloaded: partners-2025-01-13.csv
```

### Если видите ошибки:

- `Export error: No data to export` - нет данных в таблице
- `Column "..." not found in record` - неправильное имя колонки
- `Error rendering column` - ошибка в функции render

## Правильный формат CSV

Файл должен выглядеть так:

```
ID;Название;Категория;Email;Телефон;Статус
1;Partner 1;retail;partner1@example.com;+996555000001;Активен
2;Partner 2;restaurant;partner2@example.com;+996555000002;Активен
```

**Важно:**
- Разделитель: точка с запятой (`;`)
- Каждая строка на новой строке
- Заголовки в первой строке

## Альтернативное решение: Использование библиотеки xlsx

Для создания настоящих Excel файлов (.xlsx):

```bash
npm install xlsx
```

Затем обновите `exportUtils.ts`:

```typescript
import * as XLSX from 'xlsx';

export function exportToExcel<T extends Record<string, any>>(
  data: T[],
  columns: Array<{ key: string; title: string; render?: (value: any, record: T) => string | number; }>,
  filename: string
): void {
  // Создаем рабочую книгу
  const wb = XLSX.utils.book_new();
  
  // Преобразуем данные в формат для Excel
  const wsData = [
    columns.map(col => col.title), // Заголовки
    ...data.map(record => 
      columns.map(col => {
        const value = record[col.key];
        if (col.render) {
          return col.render(value, record);
        }
        return value;
      })
    )
  ];
  
  // Создаем лист
  const ws = XLSX.utils.aoa_to_sheet(wsData);
  
  // Добавляем лист в книгу
  XLSX.utils.book_append_sheet(wb, ws, 'Sheet1');
  
  // Сохраняем файл
  XLSX.writeFile(wb, `${filename}-${dayjs().format('YYYY-MM-DD')}.xlsx`);
}
```

---

**Если проблема сохраняется:**
1. Откройте консоль браузера (F12)
2. Выполните экспорт
3. Скопируйте все логи из консоли
4. Проверьте структуру данных в таблице

