# 🚀 YESS Backend API

> Система лояльности для Кыргызстана с полностью функциональной JWT аутентификацией

[![Status](https://img.shields.io/badge/status-ready-brightgreen)]()
[![Python](https://img.shields.io/badge/python-3.9+-blue)]()
[![FastAPI](https://img.shields.io/badge/FastAPI-0.100+-green)]()
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15+-blue)]()

---

## ✅ Система аутентификации исправлена!

**Последнее обновление:** 5 ноября 2025

Система аутентификации была **полностью переработана** и теперь работает корректно:

- ✅ JWT авторизация
- ✅ Регистрация с полями: phone_number, password, first_name, last_name
- ✅ Вход через OAuth2PasswordRequestForm
- ✅ Эндпоинт /auth/me для получения данных текущего пользователя
- ✅ Полная документация и тесты

---

## 🎯 Быстрый старт

### 1️⃣ Настройка окружения

```bash
# Перейти в директорию
cd E:\Yess-Go-App-Backend\Yess-Money---app-master\yess-backend

# Создать .env
copy env.example .env

# Отредактировать .env:
# - DATABASE_URL=postgresql://yess_user:password@localhost/yess_db
# - SECRET_KEY=your_super_secret_key_here_minimum_32_chars
```

### 2️⃣ Установка зависимостей

```bash
# Создать виртуальное окружение (если нужно)
python -m venv venv

# Активировать
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac

# Установить зависимости
pip install -r requirements.txt
```

### 3️⃣ База данных

```bash
# Применить миграции
alembic upgrade head
```

### 4️⃣ Запуск

**Вариант A (рекомендуется):**
```powershell
.\start_fixed_auth.ps1
```

**Вариант B (вручную):**
```bash
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 5️⃣ Тестирование

```bash
# Автоматические тесты
python test_auth_api.py

# Swagger UI
http://localhost:8000/docs

# ReDoc
http://localhost:8000/redoc
```

---

## 📋 API Эндпоинты

### Аутентификация

| Метод | Эндпоинт | Описание | Требует Auth |
|-------|----------|----------|--------------|
| POST | `/api/v1/auth/register` | Регистрация | ❌ |
| POST | `/api/v1/auth/login` | Вход | ❌ |
| GET | `/api/v1/auth/me` | Текущий пользователь | ✅ |

### Другие эндпоинты

| Категория | Префикс | Описание |
|-----------|---------|----------|
| Партнеры | `/api/v1/partners` | Управление партнерами |
| Кошелек | `/api/v1/wallet` | Операции с кошельком |
| Заказы | `/api/v1/orders` | Управление заказами |
| QR-коды | `/api/v1/qr` | Генерация и сканирование QR |
| Локации | `/api/v1/locations` | Геолокация |

---

## 📚 Документация

### Основная документация

| Файл | Описание |
|------|----------|
| **[README_AUTH_FIX.md](README_AUTH_FIX.md)** | 📄 **НАЧНИТЕ ОТСЮДА!** Краткая сводка |
| **[QUICK_START.md](QUICK_START.md)** | 🚀 Пошаговая инструкция по запуску |
| **[AUTH_SETUP.md](AUTH_SETUP.md)** | 📖 Подробная документация аутентификации |
| **[AUTH_FIX_SUMMARY.md](AUTH_FIX_SUMMARY.md)** | 📋 Полный отчет об изменениях |
| **[ARCHITECTURE.md](ARCHITECTURE.md)** | 🏗️ Архитектура системы со схемами |
| **[CHANGELOG_AUTH.md](CHANGELOG_AUTH.md)** | 📝 Детальный changelog |
| **[COMMANDS.txt](COMMANDS.txt)** | 💻 Быстрые команды для терминала |

### Swagger UI

После запуска сервера откройте: **http://localhost:8000/docs**

---

## 🔧 Структура проекта

```
yess-backend/
├── app/
│   ├── api/v1/              # API эндпоинты
│   │   ├── auth.py          # Аутентификация
│   │   ├── partner.py       # Партнеры
│   │   └── ...
│   ├── services/            # Бизнес-логика
│   │   ├── auth_service.py  # Сервис аутентификации
│   │   ├── dependencies.py  # JWT декодирование
│   │   └── ...
│   ├── models/              # SQLAlchemy модели
│   │   ├── user.py
│   │   └── ...
│   ├── schemas/             # Pydantic схемы
│   │   ├── user.py
│   │   └── ...
│   └── core/                # Конфигурация
│       ├── config.py
│       ├── database.py
│       └── ...
├── alembic/                 # Миграции БД
├── tests/                   # Тесты
├── test_auth_api.py         # Автотесты аутентификации
└── requirements.txt         # Зависимости
```

---

## 🧪 Тестирование

### Автоматические тесты

```bash
# Тесты аутентификации
python test_auth_api.py

# Все тесты
pytest
```

### Ручное тестирование

#### Регистрация
```bash
curl -X POST "http://localhost:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{"phone_number":"+996555123456","password":"test123","first_name":"Иван","last_name":"Иванов"}'
```

#### Вход
```bash
curl -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=%2B996555123456&password=test123"
```

#### Получение данных пользователя
```bash
curl -X GET "http://localhost:8000/api/v1/auth/me" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 🔐 Безопасность

### Реализовано

- ✅ Хеширование паролей (bcrypt)
- ✅ JWT токены с подписью
- ✅ Проверка срока действия токенов
- ✅ Валидация активности пользователя
- ✅ CORS настройки
- ✅ Rate limiting
- ✅ Security middleware

### Для продакшена

**⚠️ ОБЯЗАТЕЛЬНО:**
1. Установите длинный SECRET_KEY (минимум 32 символа)
2. Используйте HTTPS
3. Настройте правильные CORS_ORIGINS
4. Включите rate limiting
5. Настройте логирование
6. Используйте сильные пароли для БД

---

## 🐳 Docker

### Запуск PostgreSQL

```bash
docker run --name yess-postgres \
  -e POSTGRES_USER=yess_user \
  -e POSTGRES_PASSWORD=secure_password \
  -e POSTGRES_DB=yess_db \
  -p 5432:5432 \
  -d postgres:15
```

### Полный стек (если настроен docker-compose)

```bash
docker-compose up -d
```

---

## 🛠️ Разработка

### Создание миграции

```bash
alembic revision --autogenerate -m "описание изменений"
```

### Применение миграций

```bash
alembic upgrade head
```

### Откат миграции

```bash
alembic downgrade -1
```

### Форматирование кода

```bash
black app/
isort app/
```

### Линтинг

```bash
flake8 app/
pylint app/
```

---

## 📊 Технологии

- **FastAPI** - Современный веб-фреймворк
- **PostgreSQL** - Реляционная база данных
- **SQLAlchemy** - ORM
- **Alembic** - Миграции БД
- **Pydantic** - Валидация данных
- **JWT** - Аутентификация
- **Bcrypt** - Хеширование паролей
- **Uvicorn** - ASGI сервер

---

## 🌍 Локализация

Проект настроен для Кыргызстана:

- 🇰🇬 Страна: Кыргызстан (KG)
- 💰 Валюта: Сом (KGS)
- 🕐 Часовой пояс: Asia/Bishkek
- 📞 Формат телефона: +996XXXXXXXXX
- 📅 Формат даты: DD.MM.YYYY

---

## ❓ Часто задаваемые вопросы

### Сервер не запускается
```bash
# Проверьте зависимости
pip install -r requirements.txt

# Проверьте .env
cat .env

# Проверьте PostgreSQL
psql -U yess_user -d yess_db
```

### Ошибка при миграции
```bash
# Проверьте подключение к БД
alembic current

# Пометить текущую версию
alembic stamp head

# Применить миграции
alembic upgrade head
```

### Токен не работает
- Проверьте формат: `Bearer <token>`
- Убедитесь, что токен не истек (30 минут)
- Проверьте SECRET_KEY в .env

---

## 📞 Поддержка

При возникновении проблем:

1. **Прочитайте документацию** - начните с [README_AUTH_FIX.md](README_AUTH_FIX.md)
2. **Запустите тесты** - `python test_auth_api.py`
3. **Проверьте логи** - они покажут детали ошибки
4. **Проверьте .env** - убедитесь в правильности настроек

---

## 🎯 Дорожная карта

### Ближайшие обновления

- [ ] Refresh tokens
- [ ] SMS верификация
- [ ] Email верификация
- [ ] Восстановление пароля
- [ ] Двухфакторная аутентификация
- [ ] Социальные входы

### В разработке

- [ ] Админ-панель
- [ ] Аналитика
- [ ] Push-уведомления
- [ ] Геолокация в реальном времени

---

## 📄 Лицензия

[Указать вашу лицензию]

---

## ✨ Статус

**✅ Система аутентификации готова к использованию!**

Все эндпоинты работают корректно:
- ✅ POST /api/v1/auth/register
- ✅ POST /api/v1/auth/login
- ✅ GET /api/v1/auth/me

**Для запуска:**
```powershell
.\start_fixed_auth.ps1
```

**Для тестирования:**
```bash
python test_auth_api.py
```

**Документация API:**
http://localhost:8000/docs

---

**Приятной работы!** 🚀

