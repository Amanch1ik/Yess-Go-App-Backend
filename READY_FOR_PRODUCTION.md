# ✅ Готовый продукт - Инструкции по запуску

## 🎯 Что было сделано

### 1. Виртуальная монета Yess!Coin ✅
- ✅ Модель Wallet расширена с полем `yescoin_balance`
- ✅ API endpoints для получения и синхронизации баланса
- ✅ Миграция БД создана
- ✅ Сервис синхронизации для frontend

### 2. Синхронизация между сайтом и приложением ✅
- ✅ WebSocket для реал-тайм обновлений
- ✅ API endpoint `/api/v1/wallet/sync` для синхронизации
- ✅ Сервис `walletSyncService` для автоматической синхронизации

### 3. Улучшение карт (как в 2ГИС) ✅
- ✅ Сервис `mapService` с функциями:
  - Поиск адресов (Nominatim)
  - Обратное геокодирование
  - Построение маршрутов (OSRM)
  - Поиск ближайших партнеров
  - Форматирование расстояний и времени

### 4. Интеграция бэкенда с панелями ✅
- ✅ Все API endpoints подключены
- ✅ WebSocket работает
- ✅ Экспорт данных работает

## 🚀 Запуск

### 1. База данных

```bash
# Перейти в директорию бэкенда
cd yess-backend

# Запустить миграцию
alembic upgrade head

# Или если миграция еще не применена:
alembic revision --autogenerate -m "add yescoin to wallet"
alembic upgrade head
```

### 2. Бэкенд

```bash
cd yess-backend

# Установить зависимости (если еще не установлены)
pip install -r requirements.txt

# Запустить сервер
uvicorn app.main:app --reload --port 8000
```

### 3. Frontend панели

#### Admin Panel:
```bash
cd admin-panel
npm install
npm run dev
```

#### Partner Panel:
```bash
cd partner-panel
npm install
npm run dev
```

## 📋 Проверка работы

### 1. Проверить баланс кошелька:
```bash
curl http://localhost:8000/api/v1/wallet/balance?user_id=1
```

### 2. Синхронизировать баланс:
```bash
curl -X POST http://localhost:8000/api/v1/wallet/sync \
  -H "Content-Type: application/json" \
  -d '{"user_id": 1, "device_id": "test-device"}'
```

### 3. Проверить WebSocket:
- Откройте консоль браузера
- Должны быть сообщения о подключении WebSocket
- При изменении баланса должно прийти событие `balance_update`

## 🔧 Использование новых функций

### Синхронизация баланса в компоненте:

```typescript
import { walletSyncService } from '../services/walletSyncService';
import { useEffect } from 'react';

// В компоненте
useEffect(() => {
  const userId = 1; // Получить из контекста/состояния
  
  // Начать автоматическую синхронизацию каждые 30 секунд
  walletSyncService.startAutoSync(userId, 30000);
  
  return () => {
    walletSyncService.stopAutoSync();
  };
}, []);
```

### Использование карт с поиском:

```typescript
import { searchAddress, buildRoute, findNearestPartners } from '../services/mapService';

// Поиск адреса
const results = await searchAddress('Бишкек, Чуйкова 169');

// Построение маршрута
const route = await buildRoute(
  [42.8746, 74.5698], // Начальная точка
  [42.8846, 74.5798], // Конечная точка
  'driving' // или 'walking', 'cycling'
);

// Поиск ближайших партнеров
const nearest = findNearestPartners(
  [42.8746, 74.5698], // Позиция пользователя
  partners, // Массив партнеров
  5000 // Максимальное расстояние в метрах
);
```

## 📝 Важные замечания

1. **Миграция БД обязательна** - без нее новые поля не будут работать
2. **WebSocket требует токен** - убедитесь, что токен передается правильно
3. **Карты используют публичные API** - для production рекомендуется свой сервер OSRM
4. **Синхронизация работает автоматически** - но можно вызывать вручную

## 🐛 Troubleshooting

### Проблема: Баланс не синхронизируется
- Проверьте, что миграция БД применена
- Проверьте токен авторизации
- Проверьте логи бэкенда

### Проблема: Карты не работают
- Проверьте интернет соединение
- Проверьте CORS настройки
- Убедитесь, что Leaflet загружен

### Проблема: WebSocket не подключается
- Проверьте URL WebSocket сервера
- Проверьте токен в localStorage
- Проверьте настройки CORS на бэкенде

## 🎉 Готово к использованию!

Все основные функции реализованы и готовы к использованию. Проект готов к production развертыванию.

